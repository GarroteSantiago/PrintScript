# Testkit Module

The testkit module contains shared test helpers and fixtures.

Responsibilities:

- reusable source fixtures
- diagnostic assertions
- token and AST assertion helpers
- command-result assertion helpers

This module is for tests only. Production modules should not depend on it.
