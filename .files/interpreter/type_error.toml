[case]
name = "interpreter stops before execution on semantic type error"
command = "execute"
source = "type_error.pisp"
version = "1.0"
success = false

[[expected.diagnostics]]
phase = "SEMANTIC"
severity = "ERROR"
message = "Cannot assign string to number"
