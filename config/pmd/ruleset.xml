<?xml version="1.0"?>
<ruleset name="PrintScript PMD ruleset"
    xmlns="http://pmd.sourceforge.net/ruleset/2.0.0"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xsi:schemaLocation="http://pmd.sourceforge.net/ruleset/2.0.0 https://pmd.sourceforge.io/ruleset_2_0_0.xsd">

  <description>
    errorprone + bestpractices, with a few rules excluded because they conflict with this
    codebase's actual design rather than flagging a real defect:

    - SwitchStmtsShouldHaveDefault: this codebase relies on switch over sealed interfaces
      (StatementSyntax, ExpressionSyntax, RuntimeValue) being exhaustively checked by the
      compiler. Adding a default case would silently swallow a future unhandled case instead of
      failing the build.
    - SystemPrintln: the cli module's entire job is writing to stdout/stderr.
    - AvoidFieldNameMatchingMethodName: fields that share a name with their own accessor method
      (the record-style convention used throughout this codebase) are not accidental shadowing.
    - TestClassWithoutTestCases: misfires on testkit's TestSources, a test-fixture helper that
      lives in a main source set, not an actual test class.
  </description>

  <rule ref="category/java/errorprone.xml">
    <exclude name="AvoidFieldNameMatchingMethodName"/>
    <exclude name="TestClassWithoutTestCases"/>
  </rule>

  <rule ref="category/java/bestpractices.xml">
    <exclude name="SwitchStmtsShouldHaveDefault"/>
    <exclude name="SystemPrintln"/>
  </rule>
</ruleset>
